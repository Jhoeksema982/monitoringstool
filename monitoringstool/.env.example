# Copy this file to `.env` and fill in values before deploying.

# -----------------------------
# Frontend (Vite) environment
# -----------------------------
# Vite vars are embedded at build time and MUST start with `VITE_`.

# URL the frontend uses to call your backend API (include trailing `/api`)
# Local example: http://localhost:5000/api
# Production example (Vercel): https://your-project.vercel.app/api
VITE_API_BASE_URL=http://localhost:5000/api

# Supabase client values (public/anon)
VITE_SUPABASE_URL=https://your-supabase-url.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# Comma-separated admin emails used client-side (optional)
VITE_ADMIN_EMAILS=admin@example.com


# -----------------------------
# Server (backend) environment
# -----------------------------
# These values are used by serverless functions / Express server.
# Do NOT prefix with VITE_ for server-only secrets.

# Supabase server values
SUPABASE_URL=https://your-supabase-url.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Admin emails allowed to perform admin actions (comma-separated)
ADMIN_EMAILS=admin@example.com

# CORS allowed origins (comma-separated)
# Local dev: http://localhost:5173
# Vercel prod: https://your-project.vercel.app
CORS_ORIGIN=http://localhost:5173

# Server port (used for local development)
PORT=5000

# Session secret for server (generate a random value)
SESSION_SECRET=your-super-secret-session-key-change-in-production


# -----------------------------
# Optional / tuning
# -----------------------------
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100
NODE_ENV=development
LOG_LEVEL=info

# Notes:
# - Mark server secrets (`SUPABASE_SERVICE_ROLE_KEY`, `SESSION_SECRET`, `ADMIN_EMAILS`) as protected in Vercel.
# - `VITE_` variables are required at build time. Changing them requires a redeploy.
# - In Vercel, set Production values under Project Settings → Environment Variables.
